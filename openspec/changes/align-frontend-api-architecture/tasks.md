# Tasks

- [x] Add shared public API metadata projection and generated Web Wrangler config.
- [x] Wire Web dev, SSR, build and Wrangler types to the generated config without mutating tracked config.
- [x] Align deploy environment loading and process cleanup for root and direct frontend commands.
- [x] Keep frontend-only model types separate from API wire DTOs and update package guidance.
- [x] Update preflight, CI and frontend documentation for the merged API architecture.
- [x] Add focused tests for config projection, state paths, profile selection and missing configuration.
- [x] Run strict validation, app checks, and the three review passes from REVIEW.md.
