# Tasks

## 1. Token layer

- [x] 1.1 Add the eleven `--slax-code-*` declarations (design D1 table) to all three blocks of `apps/web/styles/theme.tokens.css`: the incumbent palette in `:root` and in `[data-slax-theme='dark']` (`--slax-code-bg: #282c34`, `--slax-code-text: #abb2bf`, `--slax-code-border: transparent`, and the eight syntax values), and the paper/grayscale palette in `[data-slax-theme='eink']` (`#ffffff` surface, `#000000` text, `#cccccc` border, grayscale syntax values). Declarations only, one per line — no selectors beyond the existing theme blocks. Verify: `pnpm web -- test tests/theme` stays green (the file's no-global-rule guard must still pass) and each block reads back against the D1 table.
- [x] 1.2 Register the family in `apps/web/tests/theme/tokens.spec.ts` by appending all eleven tokens to `ROOT_REQUIRED_TOKENS` and to `THEME_REQUIRED_TOKENS`. Verify: temporarily delete one declaration from the e-ink block and confirm `pnpm web -- test tests/theme/tokens.spec.ts` fails naming that token, then restore it.

## 2. Stylesheet

- [x] 2.1 Create `apps/web/styles/code-highlight.css`: `pre.code-block-wrapper` paints `--slax-code-bg` with a `1px solid var(--slax-code-border)` border and carries the block padding, `code.hljs` inside it is transparent with no padding, `.hljs` sets `color: var(--slax-code-text)`, and each syntax class group maps to its token using the class inventory in design D2 (including the `meta .hljs-string` and `.hljs-class .hljs-title` descendant selectors and the `emphasis` / `strong` / `link` decoration rules). The file must contain no color literal — every color is a `var(--slax-code-*)` reference. Verify: read the file against the D2 inventory, and confirm `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(|color-mix\(' apps/web/styles/code-highlight.css` matches nothing.
- [x] 2.2 Wire it globally by adding `@import './code-highlight.css';` to `apps/web/styles/theme.css`, next to the existing token import, and keep it out of `theme.tokens.css` (that file is injected into the reader's iframe and is guard-tested to be declarations-only). Verify: `pnpm web -- build` succeeds and the built stylesheet contains both a `--slax-code-bg` declaration and a `.hljs-keyword` rule; `grep -n 'code-highlight' apps/web/styles/theme.tokens.css` matches nothing.

## 3. Consumers

- [x] 3.1 Remove `import 'highlight.js/styles/atom-one-dark.css'` from `apps/web/app/components/Markdown/MarkdownText.vue` and the matching `vi.mock('highlight.js/styles/atom-one-dark.css', …)` line in `apps/web/tests/unit/components/Markdown/MarkdownText.spec.ts`. Verify: `pnpm web -- test tests/unit/components/Markdown` passes and no `highlight.js/styles` import remains in that component.
- [x] 3.2 In `apps/web/app/components/Snapshot/SnapshotChatPanel.vue`, replace the `color-mix(in srgb, var(--slax-text) 6%, transparent)` background in the code-block `:deep(pre)` rule with `var(--slax-code-bg)`, keep its padding / radius / overflow geometry, and leave the mermaid `div.mermaid-diagram` `background: #ffffff` rule untouched (design D7). Verify: the component's existing tests pass (`pnpm web -- test tests/unit/components/Chat`), and reading the rule shows the background is the token while the mermaid backdrop is still the literal.

## 4. Guard tests

- [x] 4.1 Extend `apps/web/tests/theme/tokens.spec.ts` with the e-ink code assertions: every e-ink syntax value is a gray (red, green and blue components equal); the contrast ratio of each e-ink syntax value against the e-ink `--slax-code-bg` is at least 4.5:1; the e-ink `--slax-code-bg` / `--slax-code-text` / `--slax-code-border` equal the e-ink `--slax-surface` / `--slax-text` / `--slax-border` values; and no e-ink code value contains `rgba(` or `color-mix(`. Verify: set `--slax-code-comment` to `#777777` and confirm the contrast assertion fails (it measures 4.48:1), then restore `#666666`.
- [x] 4.2 Add `apps/web/tests/theme/code-highlight.spec.ts` asserting that (a) no `highlight.js/styles/*` import exists anywhere under `apps/web/app` or `apps/web/styles`, and (b) every `var(--slax-code-*)` name referenced by `code-highlight.css` is declared in all three blocks of `theme.tokens.css`. Verify: temporarily reinstate the vendor import in `MarkdownText.vue` and confirm assertion (a) fails, then remove it again.

## 5. Verification

- [x] 5.1 Run `pnpm web -- test`, the web typecheck and lint in the worktree, all green; run `pnpm extension -- test` as a regression check (the extension has its own stylesheet import and no e-ink theme, and the shared parse layer is untouched by this change).
- [x] 5.2 Manual three-theme check on the reader page (`pages/b/[id]`) with an AI answer containing a fenced Python or TypeScript block: in light and dark the colors match the pre-change rendering with only the documented tighter inset (design D6); in e-ink the block shows a white surface, a visible 1px border, black body text, grayscale-only syntax differentiation, and no dark-filled region anywhere; and the chat block's surface, border and body text match the article code block rendered beside it on the same page. Attach the e-ink screenshot to the PR.
  - Verified by the operator on the local reader page with a real AI answer in e-ink (paper surface, 1px border, grayscale syntax, no dark fill); the before/after pair is committed under `evidence/` and embedded in the PR body. Additional CSS-level evidence: headless Chromium on the real page reported e-ink `background: rgb(255, 255, 255)`, `color: rgb(0, 0, 0)`, `border: 1px solid rgb(204, 204, 204)` — identical to the article code block — while light/dark kept their pre-change computed colors.
- [x] 5.3 Confirm the other consumer of the shared markup still renders correctly: the AI outline panel that uses `MarkdownText.vue` shows a themed code surface (not an unbacked transparent block), and a mermaid failure fallback still displays its code block with the new surface (force it with a ```` ```mermaid ```` block whose syntax is invalid, or reuse the existing fallback fixture).

## 6. Review follow-up (PR #158)

- [x] 6.1 Fix the light-theme regression the review found: the language label emitted inside the wrapper inherited the page body text color, which became `#1a1814` on `#282c34` (≈1.27:1) once the wrapper became the paint layer. `.code-block-header` now takes `--slax-code-text`; the token guard asserts text-on-surface contrast in every theme and the source guard asserts the label rule; the accepted delta is recorded in design D3/D6 and the proposal.
- [x] 6.2 Tighten the vendor-import guard: exempt only the one file that actually imports a vendor sheet (`Chat/ChatBot.vue`), scan `tests/` and `nuxt.config.ts` as well, and point the failure message at the exemption list. Register the eleven new tokens in `fork-tokens.spec.ts`'s hand-maintained upstream list so RULE B keeps covering them.
- [x] 6.3 Make the token colors actually win on the `MarkdownText.vue` surface: its blanket `:deep(*)` rule compiles to (0,3,0) and outranked the stylesheet's (0,1,x), so on that surface the code text *and* the label kept the page text color (1.27:1 on the code background in light). The blanket rule now splits: `margin` for every child, page text color only for non-code-block nodes — and the exclusion is wrapped in `:where()`, because a bare `:not()` chain raises the selector to (0,7,0) and then outranks the component's own link rules (`:deep(a)` (0,3,1) / `:deep(a:not(.slax_link))` (0,4,1)), stripping `--slax-link` from ordinary links (e-ink lost `#5490c2`). Verified in Chromium against the compiled selector shape: original → code/label page-colored, links themed; first cut → code/label themed but links page-colored; `:where()` version → links themed *and* code/label themed (label 6.57:1, keyword 4.75:1). A guard pins the `*:where(:not(...))` shape.
- [x] 6.4 Make code text and code background travel together (the same failure surfaced three times): the base `.hljs` rule now paints `--slax-code-bg` itself, with a wrapper-scoped rule making it transparent inside `pre.code-block-wrapper` (one-layer rule intact), so pre-rendered highlight markup without a wrapper is self-consistent; and the chat panel's `:deep(pre)` rule — which paints *every* `<pre>` — now also sets `--slax-code-text`, so a bare `<pre>` (mermaid placeholder, indented code block, raw HTML) is readable. Guarded by two spec assertions plus a source guard on the panel rule. (Found in review of the third revision.)
