## 1. Evidence for the decision (complete before implementation)

- [x] 1.1 Establish the job-conclusion signature of every outcome the gate must classify, from real runs and from the compiled conditions rather than from expectation — recorded in `design.md` D2. The decisive reading: every *decline* collapses to `activation: skipped` — the pause, fork, draft and stack checks are compiled onto `pre_activation` (`pr-review.lock.yml:1730`, `pr-review-command.lock.yml:1752`) and the membership path reaches `activation`'s own `activated != 'true'` condition (`pr-review.lock.yml:87`, `pr-review-command.lock.yml:89`) — while a *stop* fails a stage (`activation: failure`), and the budget guardrail is the only thing gating `agent` (`pr-review.lock.yml:394`, `pr-review-command.lock.yml:416`). A sweep of the last 200 runs of both workflows (91 non-success) found 69 all-skipped declines, 19 review failures on findings, one detection block, and exactly one `activation: failure`
- [x] 1.2 Confirm the failure the change is written for from both sides — review side, run 35490678495 (`pr-review-command.lock.yml`, event `issue_comment`): `pre_activation` success, **`activation` failure** at the step `Check workflow lock file` (the `ERR_CONFIG: E009 CONFIG_HASH_MISMATCH` path, confirmed in gh-aw v0.88.7's `check_workflow_timestamp_api.cjs` as the only thing that sets `stale_lock_file_failed` *and* fails the job), `agent`/`detection`/`review_verdict`/`safe_outputs` skipped, run conclusion `failure`. Gate side, run 35490745228: its log reads `agent=skipped verdict=skipped -> success (Review skipped (paused, fork pull request, or budget cap))` with `reviewed=` empty, and it posted `success` at 05:04:36Z — the "no review published" fail-closed branch is only reachable when `agent=success`
- [x] 1.3 Confirm the fork signature rather than assuming it — the fork drill's run 35429105440 (PR #41) has every job `skipped`, including `pre_activation` and `activation`, as archive task 4.2 records. This is the row the handoff's first sketch (`activation: success && agent: skipped` is the only legitimate skip) would have broken, since it would have failed every fork pull request. The same all-skipped shape is confirmed against real runs for the pause drills (35409308207 on #23, 35428879403 on #40) and the draft drill (35431104953 on #48)
- [x] 1.4 Confirm that the two skip descriptions can be told apart, so the gate stops guessing a cause — from 1.1: a declined run cannot be the budget cap, and an activated run with a skipped agent cannot be the pause, a fork, or a draft
- [x] 1.5 Record the adjacent defect found while gathering evidence, and keep it out of this change — PR #66 carries two `/review` comments five minutes apart by the same member: `/review\r\n` at 06:47:48 matches none of the three forms gh-aw v0.88.7 compiles for the command (run 35495160078 skipped every stage) and the plain `/review` at 06:52:45 reviewed normally (run 35495375114). The gate reported faithfully — the run *was* declined, not stopped — so the defect is upstream in which request bodies gh-aw accepts (`pkg/workflow/command.go`: no trimming, no frontmatter knob). Decided on review: leave it, recorded in `proposal.md`'s Non-Goals and `design.md`'s second open question rather than turned into work
- [x] 1.6 Confirm that no ruleset change is needed — the status context stays `PR review gate` and the same workflow keeps reporting it, so the `protect` ruleset entry is untouched and no window exists in which the required check is unreported

## 2. Gate implementation

- [x] 2.1 Read `pre_activation` and `activation` from the same `actions/runs/<id>/jobs` response the gate already fetches (`pr-review-gate.yml`), defaulting an absent job to a value that cannot pass, in the same spirit as the existing `agent=${agent:-missing}` — done: the one jobs call now also extracts `steps`, a `conclusion()` helper reads all four jobs, and `pre`/`activation` default to `missing` so an absent job cannot satisfy the passing shape
- [x] 2.2 Replace the single `case "$agent"` with the whitelist from `design.md` D1: `activation == skipped` (with `pre_activation` `success` or `skipped`) → passing skip; `pre_activation == success && activation == success` → today's `agent`/`review_verdict` logic unchanged; anything else → failure
- [x] 2.3 Replace the three-way skip string per D5 with descriptions that name only causes the gate can verify — reading the pause from `vars.PR_REVIEW_ENABLED` in the gate's own expressions, so `Review not run (reviews are paused)`, `Review not run (not activated for this run)`, and `Review not run (budget cap)` — and delete the old string, which reported a pause, a fork and a budget cap on this change's own draft pull request (#69, run 35501003126) where none of the three was true
- [x] 2.4 Build the failure description per D3: name the stage that is neither `success` nor part of the passing shape, and substitute the single failed step's name when the jobs response carries exactly one, falling back to the stage when it carries none or several; keep every description inside the 140-character status limit — done via `describe_stopped_stage()`, with the step name truncated to 50 characters; the longest message it can produce is 116 characters, measured
- [x] 2.5 Update the workflow's header comment — the repository's only committed documentation of the gate, since `REVIEW.md` carries review instructions only — so it states the whitelist and the two skip descriptions instead of describing the old two-job rule; note there that the pause, fork, draft and stack conditions live on `pre_activation`, because that is the fact the rule rests on
- [x] 2.6 Leave the reported context, the workflow name, and the `workflow_run` trigger list untouched, and check that the diff touches nothing outside `.github/workflows/pr-review-gate.yml` and this change's directory — verified: `CONTEXT`, `on.workflow_run`, `permissions` and the routing `case "$WF"` are unchanged, and the diff is the workflow plus this change's artifacts

## 3. Verification

- [x] 3.1 Verify the classification offline against real runs by replaying the gate's script against historical run ids with the final status POST replaced by a print, so no status is written: run 35490678495 must now produce `failure` naming the activation stage (previously `success`), and a completed review run must still produce `success — Reviewed: …` — done, and the A/B is the evidence: the incident run yields `failure — Review did not start (activation failed at 'Check workflow lock file'); failing closed` under this change and `success — Review skipped (paused, fork pull request, or budget cap)` under `origin/main`'s script. The clean review of #67 (run 35499204394) still yields `success — Reviewed: no Important findings`, and the findings-failure run 35431067850 still yields `failure — Review reported Important findings` under both
- [x] 3.2 Verify the passing-skip branch the same way against the recorded all-skipped runs — the pause drills (35409308207, 35428879403), the fork drill (35429105440), and the declined `/review` run (35495160078) — confirming they stay `success` with the not-activated description. These are real paused and forked runs, so this replay is the evidence for that branch rather than a claim that the signature "should" be the same — done: the pause drill, the fork drill, the draft run 35501003126 and the declined `/review` run all yield `success — Review not run (not activated for this run)`, and re-running the pause drill with `PAUSED=true` yields `success — Review not run (reviews are paused)`. The routing half is unchanged too: the on-demand wrapper's `pull_request` run (35499204404) still posts nothing, and the detection-block run 35436001473 still yields `failure — No review published for this run; failing closed`
- [x] 3.3 Record which rows the replay cannot reach: the daily budget-guardrail row has never fired (the guardrail computes `aic: 0` for this model — archive task 2.6) and the membership tuple has never occurred, so both are verified from the compiled condition only, and the limitation is stated rather than papered over — recorded in `design.md` D2, which marks the membership row as the only passing tuple without runtime evidence and the budget rows as read off the compiled `if:`
- [x] 3.4 Open the implementation pull request with an `OpenSpec: fail-gate-when-review-did-not-start` line and a check that `pnpm agent:check` (rulesync doctor/generate, `openspec validate --all --strict`, the gh-aw drift guard) is green — this pull request; `pnpm agent:check` passes
- [ ] 3.5 After merge, run the live drill the change needs: a throwaway pull request whose branch carries a deliberately stale compiled workflow, so its own review run fails `activation` at `Check workflow lock file` before the agent starts (zero model cost, the same mechanism as the incident) — the merged gate must report `failure` on that pull request, not `success — Review skipped`. Close it without merging, as the earlier drills (#29, #59) were closed
- [ ] 3.6 After merge, confirm the automatic path still reports normally on a real pull request — `success — Reviewed: …` or `failure — Review reported Important findings` — so the stricter rule did not disturb the ordinary verdict path

## 4. Follow-ups outside this change

- [ ] 4.1 Re-read the decision rule in `design.md` D1/D2 at the next gh-aw pin bump: the whitelist depends on *where* gh-aw compiles the pause, fork, draft and stack conditions, on the membership path reaching `activation` rather than `pre_activation`, and on the stale-lock check failing `activation`. A release that moves any of them can turn a real failure into a shape that reads like a decline (see the residual risk in `proposal.md`). This is a task rather than a comment because a pin bump is the only moment the question can be answered
- [ ] 4.2 Reconsider 1.5's declined-`/review` limitation if gh-aw ever accepts a trailing CRLF, or if `/review\r\n` is seen again in practice. The durable fix is upstream; the workflow-side alternative is to replace the `slash_command` trigger, which archive design D3 rejected once, so this stays closed until either the upstream matcher changes or the failure is observed to matter
