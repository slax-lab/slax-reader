# Tasks

实现与验证分开记录：代码交付见 verification.md；任务同时要求平台/集成测试而尚未执行时，保持未勾选。用户已授权先实现，不再为流程暂停。

## 1. 实现基线与契约

- [x] 1.1 在提案审阅通过后，将已审阅的 `apps/api` 迁移基线接入本任务工作树，记录来源 commit 和依赖 PR；以 git diff 确认未混入其他工作树的未提交修改及无关 OAuth 变更。
- [ ] 1.2 为 Web 与原生 APP 分别准备任务工作树，保留各自已有未提交修改；读取实际 package/Gradle 脚本，安装依赖并记录基线检查结果及真实环境限制。
- [ ] 1.3 在 Workers 测试运行时验证 Feedsmith 与 sanitize-html 的解析/净化样例和生产 bundle，锁定通过验证的发布版本；验证无外部实体加载且超大/深层输入受限。
- [x] 1.4 在 `packages/contracts` 定义 RSS DTO、错误码、游标和收藏结果，并交付三端共同使用的 JSON fixtures；以类型检查和序列化测试确认不透明 ID、可空日期和分页字段一致。

## 2. 后端订阅存储与实验室

- [x] 2.1 按 API 现有 Prisma/迁移格式增加订阅与条目表、唯一约束、索引及显式生命周期清理；在临时测试数据库验证迁移、重复写入、取消订阅与共享缓存清理。
- [ ] 2.2 实现按用户隔离的 repository、20 个订阅配额及事务化重复添加；通过双用户与并发添加测试验证归属和上限。
- [x] 2.3 在现有 Labs 注册 RSS 并增加服务端访问检查；更新实验室注册测试，验证默认关闭、跨端开关读取及既有 YouTube 行为不变。
- [ ] 2.4 实现删除订阅、账户删除和关闭开关时的任务失效处理；通过运行中抓取与删除/关闭竞争测试验证旧任务不能恢复数据。

## 3. Feed 获取、解析与后台更新

- [ ] 3.1 实现受限 Feed HTTP adapter，复用 publicFetch、条件请求和跨源头处理；测试公共 URL、内网/重定向拒绝、超时、解压后大小、304、429 和 Retry-After。
- [x] 3.2 实现 RSS 2.0、RDF、Atom 字段归一化、相对链接、日期、GUID/ID 与无标识回退；以有效空 Feed、内容命名空间、异常日期和重复修订 fixtures 验证结果。
- [ ] 3.3 实现首次验证添加、错误脱敏及限流，成功后写入首次缓存；验证无效地址/格式不占配额、含 query 的地址保持语义且日志不含令牌。
- [ ] 3.4 实现条目预算、单次 200 条解析/累计缓存保留 30 天和稳定分页；用受控时间、修订与翻页中新增条目测试验证去重、截断和游标边界。
- [ ] 3.5 新增 Cron 扫描和 Queue consumer，实现原子租约、有限投递、30 分钟目标周期与退避；验证重复投递、过期租约、投递失败恢复以及无商业抓取/AI 副作用。
- [x] 3.6 实现手动刷新及前端可读更新状态；测试 5 分钟冷却、有效 Retry-After、已有任务和失败保留旧缓存。

## 4. 安全图文预览与图片缓存

- [x] 4.1 实现显式 HTML 白名单、链接规范化、节点预算及安全截断；使用恶意 HTML、实体、畸形属性、深层节点、危险协议与嵌入资源样例验证净化结果。
- [x] 4.2 将相对图片、lazy src 和 srcset/picture 归一化为单一受支持图片，缩略图使用相同策略；验证输出不存在可直接请求源站的图片候选、CSS 或嵌入资源。
- [ ] 4.3 提取现有图片签名 helper，为 RSS 添加受签名保护且独立 cache key 的受限缓存模式；验证 R2/cache 命中、签名篡改、类型/3 MiB 限制以及所有失败路径无源站重定向。
- [ ] 4.4 回归既有书签图片与视频代理行为；以现有代理测试确认旧 URL、Range 和正常缓存不被 RSS 模式改变。

## 5. 收藏与 HTTP 接口

- [ ] 5.1 增加从已授权 RSS 条目收藏的幂等 domain 入口，返回真实书签关系标识；测试新建、并发/重试、已归档、已删除恢复、缺少原文及跨用户访问。
- [x] 5.2 接入现有 URL 正文工作流及启动失败补偿，保持旧 add_url 契约；注入数据库提交后工作流启动失败，验证恢复后只产生一个书签和一次有效处理。
- [ ] 5.3 实现列表/详情批量查询当前收藏状态；验证 Inbox 删除、恢复和归档后状态正确，且删除 RSS 缓存不影响书签正文。
- [ ] 5.4 按 design 中契约完成订阅、条目、刷新和收藏 controllers，并生成 route/DI/Cron/consumer 注册；通过 HTTP 集成测试验证认证、开关、归属、错误 envelope、游标与 ID 类型。

## 6. Web

- [x] 6.1 在 fork 层加入 RSS API wrapper/types 与状态管理，沿用现有 Labs composable；以共同契约 fixtures 和状态测试验证错误、取消请求及账号切换清理。
- [ ] 6.2 在实验室与导航加入 RSS 入口、开关和现有支持语言文案；验证关闭时入口不可用、开启后可发现、旧实验室行为不变。
- [ ] 6.3 实现订阅添加/删除、来源选择、分页列表与刷新状态，复用现有基础 UI；组件测试覆盖加载、空列表、配额、失败、冷却和快速切换。
- [ ] 6.4 实现安全图文预览、打开原文、收藏和打开书签；交互测试确认图片仅用代理 URL、失败不回退、重复点击正确处理且成功后 Inbox 可见。
- [ ] 6.5 运行仓库实际 lint、类型检查、相关 Vitest 与构建脚本；在桌面与窄屏检查主题、语言、空态和长文，记录验证结果。

## 7. 原生 APP

- [ ] 7.1 增加 RssApi、序列化 DTO、ApiService 实现、repository 和 Koin 注册；通过共同 JSON fixtures 与 Ktor 测试验证认证、错误和字段一致性。
- [ ] 7.2 实现 RSS StateFlow ViewModel 与可序列化路由；commonTest 覆盖订阅切换、分页、重复刷新、关闭、登出、离线及生命周期恢复。
- [ ] 7.3 在原生设置和导航增加 Labs/RSS 开关及入口，沿用 LocaleString 和主题；验证 Android/iOS 入口显示及跨端开关读取。
- [ ] 7.4 使用 Compose 完成订阅管理、添加/删除对话框、文章列表与刷新状态；通过 UI 测试或平台运行验证完整交互、返回及失败重试。
- [x] 7.5 原生内容块方案曾完成；后续用户明确要求复用现有详情，已由第 16 节共享阅读器方案替代。
- [ ] 7.6 接入原生图片查看、收藏与书签导航等待；测试图片失败占位、后端已成功但 PowerSync 延迟、重复点击与本地书签到达后的打开行为。
- [ ] 7.7 运行实际 Gradle common/Android 测试与 Android、iOS 编译任务，在两个平台验证原生图文和长文滚动；记录 SDK/签名/网络限制，不将未执行检查记为通过。

## 8. 三端验收与交付

- [ ] 8.1 在受控 Feed 服务上联调开启、添加、自动更新、预览、图片缓存、收藏、Inbox 可见与删除订阅闭环；记录 Web/Android/iOS 的共同验收结果。
- [ ] 8.2 联调双用户隔离、304、429、恶意地址/HTML、图片缓存失败、关闭/删除竞争和重复收藏；验证源站图像不会被客户端直接加载，Feed 更新不会触发正文服务。
- [x] 8.3 交付运行说明，包含配置项形状、调度、缓存与配额、错误状态、部署顺序和回滚；验证不包含凭据，且按文档可定位刷新失败原因。
- [ ] 8.4 运行 `openspec validate --all --strict` 与各仓 `git diff --check`，按 REVIEW.md 完成 Bugs、Security、Compliance 三遍本地评审并解决 Important；保存实际检查与未通过项。
- [ ] 8.5 准备三端关联 PR，后端 PR 指向 dev 并注明 `OpenSpec: add-rss-labs`，关联已审阅迁移依赖与验证证据；核对每个 PR 只包含对应任务代码，合并后另行归档。


## 9. Inbox 与订阅名称跟进

- [x] 9.1 在现有 bookmarks 页面嵌入 RSS，入口紧接 Inbox，复用布局、搜索和列表切换器；组件测试验证切换与刷新不重建已有条目 DOM。
- [x] 9.2 支持自定义显示名称添加、修改和清空；API、状态和组件测试覆盖名称保留及归属检查。
- [x] 9.3 实现按来源的会话列表缓存、TTL、已加载页重新校验与失败保留；状态测试覆盖过期响应和账号清理。
- [x] 9.4 修复开发环境 HTTP 图片代理、无效缩略图导致列表失败与详情顶层节点丢失，运行定向回归。
- [x] 9.5 删除旧 RSS DDL 与本地 RSS 表，使用 `pnpm api -- gen:diff` 生成共享 Feed 和后续时间线索引迁移，并用 `pnpm api -- migration:local:pgsql` 同步本地库。
- [x] 9.7 修正页面实际读取的 `rss.*` 中英文本地化键；顶部按 Collection 来源头像布局显示 RSS 源和代理图标，文章按日期分组展示。
- [x] 9.8 为全部订阅时间线生成 `(sort_at DESC, id DESC)` 索引并同步本地库；已有单源索引保留，发布时间修正时重排条目，无日期条目维持首次发现顺序。
- [ ] 9.6 完成浏览器视觉和真实前后端闭环验收；当前工具访问未获授权。


## 10. 共享 Feed 与 R2 正文

- [x] 10.1 更新共享模型与生成的全新迁移，真实本地 PostgreSQL 执行 RSS 表重建；旧 RSS 实验数据按用户要求丢弃，原有书签表保留。
- [ ] 10.2 实现 Feed 级首次添加互斥、定时与手动刷新、用户授权和共享缓存生命周期，验证多用户、查询令牌、并发租约及失效任务。
- [ ] 10.3 复用 Imager/BucketClient，在抓取时生成代理图文并写 R2，验证写入失败保留旧引用、304 和对象回收。
- [ ] 10.4 保持三端 DTO 与收藏行为兼容，完成类型、相关回归与规范校验，并记录真实迁移/平台验收状态。

## 11. 混合历史分页

- [x] 11.1 解析显式历史链接，增加共享历史进度、租约、重试及去重保存；最新页更新保持独立。
- [x] 11.2 三端接入 has_more、cached_more 和历史状态；显式 Load more 补抓，之后重读展开窗口，失败保留列表。
- [x] 11.3 已核对生成的历史字段迁移 20260923065020 和后续空迁移 20260923071634_add_rss_history_paging，二者在本地 PostgreSQL 的迁移记录均为已应用。
- [ ] 11.4 运行历史分页回归及三端联调。用户要求暂不运行单测；迁移完成后再验证持久化路径。

## 12. 首次失败与图标文字

- [x] 12.1 新地址抓取解析成功后再建占位；首次发布失败清理本次空占位，并保护其他租约与既有缓存。
- [x] 12.2 Web 来源图标跳过标点、数字和 emoji，提取首个汉字或单词，完整名称保持不变。
- [ ] 12.3 首次失败、租约过期和多人复用的新增回归已补充，按用户要求暂不执行单测。

## 13. 第一页默认行为与商业 Feed 回退

- [x] 13.1 核对新订阅和定时更新只读源第一页、默认列表只读缓存、手动 Load more 才抓取保存历史。
- [x] 13.2 复用 Zyte 配置实现受限原始响应回退，接入首次、刷新与历史分页，保留编码、validator、大小和重定向检查。
- [ ] 13.3 已补充分页和挑战回退回归用例，按用户要求暂不执行；真实 Zyte 与数据库联调尚待完成。

## 14. APP 入口可发现性

- [x] 14.1 RSS 入口已按后续确认移到顶部来源栏，设置页仅保留实验室开关。
- [x] 14.2 Android compileDebugKotlinAndroid 编译及 OpenSpec 严格校验通过；按用户要求不运行单测。
- [ ] 14.3 Android/iOS 实机验证入口、关闭菜单及返回筛选保留行为。

## 15. 原生 RSS 复用 Inbox 界面

- [x] 15.1 设置页独立读取与保存实验室开关，不触发订阅或文章请求；内容页移除开关及解说段落。
- [x] 15.2 来源管理收进菜单、复用分隔线和圆角列表；单行方案已由后续确认的标题、摘要和缩略图布局替代。
- [ ] 15.3 完成 Android/iOS 编译和实机视觉检查；按用户要求暂不运行单测。

## 16. 顶部来源融合与现有详情页

- [x] 16.1 每个 RSS 源与我的 Inbox、Collection 平级放入同一顶部来源栏，原位切换，删除独立 RSS 首页路由和下拉入口。
- [x] 16.2 来源图标采用站点图标、文字回退和选中描边；文章显示标题、摘要和右侧缩略图。
- [x] 16.3 通过 BookmarkRoutes 的 RSS 来源模式复用现有 DetailScreen、正文引擎和 ImageViewer；不为预览创建书签。
- [x] 16.4 详情顶部显示收藏、原文，长文章末尾追加收藏；成功后原位反馈并广播更新首页缓存。
- [x] 16.5 添加和编辑订阅使用底部面板，失败保留输入和错误、成功后收起。
- [x] 16.6 Android 编译、静态差异及 OpenSpec 严格校验通过；未运行测试。
- [ ] 16.7 iOS 编译与实际显示确认；现有 Swift 构建权限问题阻塞 iOS 编译，后续不运行测试。

## 17. Consolidate the existing Web implementation

- [x] 17.1 Merge current origin/dev locally while preserving existing API and RSS changes.
- [x] 17.2 Port the legacy frontend delta into apps/web, preserving dev's flattened imports, shared contracts and local configuration architecture.
- [x] 17.3 Carry over existing regression files without executing tests; record dependency installation, static checks and remaining type-check limitations.
