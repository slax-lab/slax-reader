# Team routing verification (temporary)

This file exists only so that a pull request can produce a real `pull_request`
`opened` event for a `CODEOWNERS`-owned repository, so the Feishu notification
bridge can be checked for the team line it is supposed to render:

```
评审 @slax-lab/reader-core（团队）
```

The pull request is closed without merging and this branch is deleted.
