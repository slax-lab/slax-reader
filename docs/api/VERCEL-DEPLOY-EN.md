# Vercel deployment status

The migrated API targets Cloudflare Workers, not Vercel. No API deployment procedure for Vercel is provided. Web frontend migration is still pending; this document is not a frontend deployment guide.

Use the [Cloudflare API deployment guide](CLOUDFLARE-DEPLOY-EN.md) and check the [repository migration status](../../README.md).
