# Agentic review verification (temporary)

This file exists only to exercise the automated review pipeline end to end on a
real pull request: the automatic review, the `/review` re-run, the
`Agentic PR review` status check and the paused-notice companion. The pull
request that adds it is closed once the verification is done.

Line added to create a new head commit without a gate status.

Second line to test the paused path with the fixed gate.
